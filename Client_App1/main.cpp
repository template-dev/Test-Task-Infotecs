#include "src/BufferProcessor.hpp"

int main(int argc, char* argv[]) {

    while(true) {
        std::string str;
        std::cin >> str;
        if(str.size() > 64) {
            continue;
        }
        
    }

    return EXIT_SUCCESS;
}